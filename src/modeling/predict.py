import pandas as pd
from src.preprocess import Preprocess

class Predict:
	def __init__(self, model, categorical_features):
		# self.model = model
		# self.preprocessor = Preprocess(categorical_features)
		pass
		
	def predict(self, df):
		# df = self.preprocesser.transform(df)
		# return self.model.predict(df)
		pass